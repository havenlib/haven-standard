<tr>
    <td>{{ entity.name }}</td>
    <td>{{ entity.mimeType }}</td>
    <td>
        <ul>
            <li>
                <a href="{{ path('haven_persona_persona_edit', {'id': entity.id}) }}">edit</a>
            </li>
            <li>
                <form action="{{ path('haven_persona_persona_delete') }}" method="post">
                    {{ form_widget(delete_forms[entity.getId()]) }}
                </form>
            </li>
            <li>
                <form action="{{ path('haven_persona_persona_download', {'id': entity.id}) }}" method="post">
                    <button type="submit">download</button>
                </form>
            </li>
        </ul>    
    </td>
</tr>
